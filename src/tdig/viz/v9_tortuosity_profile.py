"""V9 — tortuosity profile by context. Diagnostic.

x = layer ell, y = mean tau(ell) per context class.
Companion to V3 curvature heatmap; reading both clarifies the
local-vs-cumulative trajectory distinction.
"""

from __future__ import annotations


def tortuosity_profile_data(tau, context_labels):
    raise NotImplementedError("S5 implementation")
